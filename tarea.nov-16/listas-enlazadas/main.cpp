#include <bits/stdc++.h>
#include "lista.h"

using namespace std;

int main(){
    lista lst;
    lst.insertar(1);
    lst.insertar(2);
    lst.insertar(3);
    lst.insertar(4);
    lst.mostrar();
    return 0;

}
